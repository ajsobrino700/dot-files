#!/bin/zsh

alias nv="nvim"

# It is mandatory avaible the font allows icons ( I install font-hack)
# brew tap homebrew/cask-fonts
# brew install --cask font-hack-nerd-font
alias ls="lsd"
alias ll="lsd -l"

alias cat="bat"

